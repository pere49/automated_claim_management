"""Tests for the claim verifier. Run from the project folder:

    .venv\Scripts\python.exe -m unittest discover -s tests -t .

GUI tests run off-screen (QT_QPA_PLATFORM=offscreen, set in tests/qt.py).
All test data is synthetic, generated inside the tests.
"""
